output.elasticsearch:
    hosts: ["http://{{ hostvars['els']['ansible_facts']['default_ipv4']['address'] }}"]
setup.kibana:
    host: "http://{{ hostvars['kbn']['ansible_facts']['default_ipv4']['address'] }}"
filebeat.config.modules.path: ${path.config}/modules.d/*.yml